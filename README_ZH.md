# NATS Control UI

现代化的 NATS 控制平面 Web 管理界面，支持多集群管理。这是 NATS Control API 的前端应用，为分布式云环境中的 NATS 集群、账户、用户和 JetStream 配置提供全面的管理能力。

使用 Vue 3、TypeScript 和 Element Plus 构建，提供专业、直观的用户体验。

[English](README.md) | 简体中文

## 功能特性

### 🌐 集群管理
- 创建、编辑和管理 NATS 集群及其配置
- 实时集群健康监控和状态跟踪
- 多集群 JWT 同步和协调
- 集群拓扑可视化和监控仪表板
- 测试集群连接和验证配置

### 🏢 账户管理
- 创建、编辑和管理 NATS 账户及详细权限
- 配置账户限制、配额和资源限制
- 高级 JetStream 配置和流管理
- 导入/导出设置用于账户间通信
- 账户状态管理（启用/禁用）及批量操作

### 👥 用户管理
- 在账户内创建和管理用户，支持基于角色的访问控制
- 详细的权限配置和细粒度控制
- 高级访问控制设置（IP 限制、时间限制、连接类型）
- 用户凭证管理和 JWT 生成
- 用户状态管理及审计跟踪

### 📊 JetStream 管理
- 创建和配置 JetStream 流和消费者
- 监控流性能和消费者健康状态
- 配置保留策略和存储设置
- 跨集群流复制
- 高级流分析和指标
- 🆕 **重试失败的创建**: 自动重试失败的 JetStream 创建，并持久化配置存储

### 📋 JWT 任务监控
- 实时 JWT 同步任务监控和实时更新
- 任务重试功能和智能重试策略
- 详细的错误报告和故障排除信息
- 任务时间线和全面的状态跟踪
- 性能统计仪表板及历史数据

### 🎨 现代化 UI 功能
- 深色/浅色主题支持，自动检测系统偏好
- 全响应式设计，适配所有设备和屏幕尺寸
- WebSocket 集成的实时数据更新
- 面包屑和搜索功能的直观导航
- 高级过滤、排序和分页
- 一键复制凭证功能，便于分享
- 快捷键支持
- 符合无障碍标准（WCAG 2.1）

## 技术架构

### 前端技术栈
- **Vue 3** - 渐进式 JavaScript 框架，使用 Composition API
- **TypeScript** - 类型安全开发，完整的 IDE 支持
- **Element Plus** - 专业的 Vue 3 组件库
- **Pinia** - Vue 的现代状态管理
- **Vue Router** - 官方客户端路由解决方案
- **Axios** - 基于 Promise 的 HTTP 客户端，支持拦截器
- **Vite** - 下一代构建工具和开发服务器
- **SCSS** - 高级 CSS 预处理器
- **ESLint + Prettier** - 代码质量和格式化

### 后端集成
- 与 NATS Control API 后端的 RESTful API 集成
- 全面的错误处理和用户反馈
- JWT 认证支持（待实现）
- 实时数据同步
- 乐观 UI 更新，提升用户体验
- 请求/响应日志和调试支持

## 快速开始

### 前置要求
- **Node.js** 18.1+ （注意：v18.0.0 与 vue-tsc 存在兼容性问题）
- **npm/yarn/pnpm** 包管理器
- **运行中的后端**: NATS Control API 后端（参见 `nats-control-api` 项目）
- **浏览器**: 支持 ES2020+ 的现代浏览器（Chrome 90+、Firefox 88+、Safari 14+、Edge 90+）

### 安装步骤

1. 进入 UI 目录：
```bash
cd nats-control-ui
```

2. 安装依赖：
```bash
npm install
# 或
yarn install
# 或
pnpm install
```

3. 配置环境（可选）：
```bash
# 如果没有 .env 文件，应用将使用默认设置
# 默认 API URL: http://localhost:8080
cp .env.example .env
# 如果 API 端点不同，编辑 .env 文件
```

### 开发

**启动开发服务器：**
```bash
npm run dev
# 或
yarn dev
# 或
pnpm dev

# 开发服务器默认会在 http://localhost:5173 启动
# 你也可以指定自定义端口：
npm run dev -- --port 3000
```

然后在浏览器中打开终端显示的 URL（默认：`http://localhost:5173`）

### 生产构建

**构建生产版本：**
```bash
# 完整构建（包含类型检查）
npm run build

# 如果遇到 vue-tsc 问题（Node.js v18.0.0），使用：
npx vite build

# 或使用其他包管理器：
yarn build
pnpm build
```

构建文件将在 `dist/` 目录中，可直接部署。

**本地预览生产构建：**
```bash
npm run preview
# 或
yarn preview
# 或
pnpm preview

# 预览服务器默认会在 http://localhost:4173 启动
```

### 可用脚本

- `npm run dev` - 启动开发服务器，支持热重载
- `npm run build` - 生产构建（包含 TypeScript 类型检查）
- `npm run preview` - 本地预览生产构建
- `npm run lint` - 代码检查并自动修复问题
- `npm run format` - 使用 Prettier 格式化代码
- `npx vite build` - 生产构建（如需跳过类型检查）

## 配置

### 环境变量

在根目录创建 `.env` 文件（可选）：

```env
# API 配置
VITE_API_BASE_URL=http://localhost:8080
VITE_API_TIMEOUT=30000

# 应用配置
VITE_APP_TITLE=NATS Control Management
VITE_APP_VERSION=1.0.0

# 开发配置
VITE_DEV_MOCK_API=false
VITE_DEV_AUTO_LOGIN=false
```

### 代理配置

开发服务器配置为代理 API 请求到后端：

```typescript
// vite.config.ts
server: {
  proxy: {
    '/api': {
      target: 'http://localhost:8080',
      changeOrigin: true,
    },
  },
}
```

## 项目结构

```
src/
├── api/                    # API 客户端模块
│   ├── client.ts          # Axios 配置和拦截器
│   ├── accounts.ts        # 账户 API 调用
│   ├── users.ts           # 用户 API 调用
│   ├── clusters.ts        # 集群 API 调用
│   ├── jetstreams.ts      # JetStream API 调用
│   └── jwt-tasks.ts       # JWT 任务 API 调用
├── components/            # 可复用的 Vue 组件
│   └── ClusterTopologyChart.vue  # 集群可视化
├── layouts/               # 布局组件
│   └── Layout.vue         # 主应用布局
├── router/                # Vue Router 配置
│   └── index.ts          # 路由定义
├── stores/                # Pinia 状态管理
│   ├── accounts.ts        # 账户 store
│   ├── users.ts           # 用户 store
│   ├── clusters.ts        # 集群 store
│   ├── jetstreams.ts      # JetStream store
│   └── theme.ts           # 主题 store
├── styles/                # 全局样式和主题
│   └── index.scss        # 主样式表
├── types/                 # TypeScript 类型定义
│   └── index.ts          # 共享类型定义
├── utils/                 # 工具函数
│   └── index.ts          # 通用工具
├── views/                 # 页面组件
│   ├── Dashboard.vue      # 主仪表板
│   ├── accounts/          # 账户管理页面
│   ├── users/             # 用户管理页面
│   ├── clusters/          # 集群管理页面
│   ├── jetstreams/        # JetStream 管理页面
│   └── jwt-tasks/         # JWT 任务管理页面
└── main.ts                # 应用入口
```

## 部署

### Docker 部署

```dockerfile
# 多阶段构建优化生产镜像
FROM node:18-alpine AS builder

WORKDIR /app
COPY package*.json ./
RUN npm ci --only=production

COPY . .
RUN npm run build

FROM nginx:alpine
COPY --from=builder /app/dist /usr/share/nginx/html
COPY nginx.conf /etc/nginx/conf.d/default.conf

EXPOSE 80
CMD ["nginx", "-g", "daemon off;"]
```

### Nginx 配置

```nginx
server {
    listen 80;
    server_name localhost;
    root /usr/share/nginx/html;
    index index.html;

    # 启用 gzip 压缩
    gzip on;
    gzip_types text/plain text/css application/json application/javascript text/xml application/xml;

    # 处理客户端路由
    location / {
        try_files $uri $uri/ /index.html;
    }
    
    # 代理 API 请求到后端
    location /api {
        proxy_pass http://nats-control-api:8080;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    # 缓存静态资源
    location ~* \.(js|css|png|jpg|jpeg|gif|ico|svg)$ {
        expires 1y;
        add_header Cache-Control "public, immutable";
    }
}
```

## 贡献指南

欢迎社区贡献！请遵循以下指南：

### 如何贡献

1. **Fork 仓库**到您的 GitHub 账户
2. **克隆您的 fork**到本地：
   ```bash
   git clone https://github.com/YOUR_USERNAME/nats-control-ui.git
   cd nats-control-ui
   ```
3. **创建功能分支**，使用描述性名称：
   ```bash
   git checkout -b feature/amazing-feature
   # 或
   git checkout -b fix/bug-description
   ```
4. **进行更改**，遵循代码风格指南
5. **彻底测试您的更改**：
   ```bash
   npm run lint
   npm run format
   npm run build
   ```
6. **提交更改**，使用清晰的描述性消息：
   ```bash
   git commit -m 'feat: 添加某个功能'
   # 或
   git commit -m 'fix: 修复用户认证问题'
   ```
7. **推送到您的 fork**：
   ```bash
   git push origin feature/amazing-feature
   ```
8. **提交 Pull Request**，包含：
   - 清晰的标题和描述
   - 相关 issue 的引用
   - 截图（如果有 UI 更改）
   - 测试结果

### 开发环境设置
1. 确保后端 API 正在运行（参见 `nats-control-api` 项目）
2. 按照上面的安装说明操作
3. 进行更改并彻底测试
4. 运行 lint 和格式化：`npm run lint && npm run format`
5. 确保所有 TypeScript 类型正确：`npm run build`

### 代码风格指南
- 遵循现有的代码模式和约定
- 所有新代码使用 TypeScript
- 为复杂逻辑添加注释
- 使用清晰、描述性的变量和函数名
- 保持组件小而专注
- 确保无障碍性（ARIA 标签、键盘导航）

### 提交信息约定
我们遵循 [Conventional Commits](https://www.conventionalcommits.org/) 规范：
- `feat:` - 新功能
- `fix:` - Bug 修复
- `docs:` - 文档更改
- `style:` - 代码风格更改（格式化等）
- `refactor:` - 代码重构
- `test:` - 添加或更新测试
- `chore:` - 维护任务

## 开源协议

本项目采用 MIT 协议 - 详见 [LICENSE](LICENSE) 文件。

Copyright (c) 2025 nats-control-ui

特此免费授予任何获得本软件及相关文档文件（"软件"）副本的人不受限制地处理软件的权利，包括但不限于使用、复制、修改、合并、发布、分发、再许可和/或销售软件副本的权利，以及允许获得软件的人这样做，但须符合以下条件：

上述版权声明和本许可声明应包含在软件的所有副本或主要部分中。

## 支持

如需支持和提问：
- 在仓库中创建 issue
- 查看 API 文档：`http://localhost:8080/swagger/index.html`
- 查看下面的故障排除指南

## 故障排除

### 常见问题

1. **API 连接失败**
   - 确保 NATS Control API 后端在 `http://localhost:8080` 运行
   - 检查前端和后端之间的网络连接
   - 验证后端的 CORS 配置
   - 检查浏览器控制台的详细错误消息

2. **构建错误**
   - 清除 `node_modules` 并重新安装：`rm -rf node_modules package-lock.json && npm install`
   - 检查 Node.js 版本兼容性（需要 18+）
   - 验证 TypeScript 配置并修复类型错误
   - 确保所有依赖兼容

3. **主题/UI 问题**
   - 清除浏览器缓存和本地存储
   - 检查浏览器是否支持 CSS 自定义属性
   - 验证 Element Plus 主题配置
   - 在不同浏览器中测试兼容性

4. **开发服务器问题**
   - 检查端口 3000 是否可用
   - 尝试使用不同端口运行：`npm run dev -- --port 3001`
   - 验证 Vite 配置和代理设置
   - 检查是否有冲突的浏览器扩展

5. **Docker 部署问题**
   - 确保 Docker 和 Docker Compose 已安装并运行
   - 检查后端服务是否可从容器访问
   - 验证 nginx 配置和代理设置
   - 查看容器日志：`docker logs <container-name>`

## 截图展示

### 仪表板概览
![仪表板](screenshots/dashboard.png)

### 集群管理
![集群列表](screenshots/cluster01.png)
![集群详情](screenshots/cluster02.png)
![集群监控](screenshots/cluster03.png)
![集群拓扑树](screenshots/cluster-detail-tree.png)

### 账户管理
![账户列表](screenshots/accounts.png)
![账户详情](screenshots/account-detail.png)

### 用户管理
![用户列表](screenshots/users.png)
![用户详情](screenshots/user-detail.png)

### JetStream 管理
![JetStream 详情](screenshots/jetstream-detail.png)
![JetStream 差异对比](screenshots/jetstream-diff.png)

### 消费者管理
![消费者列表](screenshots/consumer.png)
![消费者差异对比](screenshots/consumer-diff.png)